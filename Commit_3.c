#define _CRT_SECURE_NO_WARNINGS 
#include <stdio.h>

int sum (int a, int b) // функция вычисления суммы 
{
    int c = a + b;  
	return c;  
}

int mult (int a, int b) // функция вычисления умножения 
{
    int c = a * b; 
	return c;  е
}

int dif (int a, int b) // функция вычисления вычитания 
{
    int c = a - b;  
	return c;  
}